# Package Overview

`core` contains core `storyshots` components that is used by other `storyshots` packages and end clients.

# Structure

`./node` - contains components that directly depend on Node.js runtime.
`./neutral` - contains components that do not depend on any specific runtime.
`./browser` - contains components that directly depend on Browser runtime.
