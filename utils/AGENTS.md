# Package Overview

`utils` contains shared useful types and functions for Storyshots packages.

## Rules

* `utils` is read-only for routine feature work.
* Do not modify files under `utils` unless the user explicitly requests `utils` changes.
