{
  "name": "storyshots",
  "version": "0.0.0",
  "description": "Visual regression provider",
  "main": "index.js",
  "repository": "https://github.com/storyshots/storyshots.git",
  "license": "MIT",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "compile": "npm run compile --workspaces --if-present",
    "pack": "npm pack --workspaces"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^22.10.2",
    "cross-env": "^7.0.3",
    "eslint": "^9.36.0",
    "eslint-plugin-react": "^7.37.5",
    "prettier": "3.1.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "tsup": "^8.4.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0"
  },
  "workspaces": [
    "packages/core",
    "packages/arrangers",
    "packages/*",
    "examples/*",
    "tests"
  ],
  "engines": {
    "node": ">=22.12.0"
  }
}
